# monte-carlo

Simple Rust workspace with Monte Carlo experiments for estimating constants.

## Crates

- `euler`: estimates Euler's number, `e`
- `pi`: estimates pi using random points in a unit square

## Run

```sh
cargo run -p euler
cargo run -p pi
```

## Check

```sh
cargo check --workspace
```
